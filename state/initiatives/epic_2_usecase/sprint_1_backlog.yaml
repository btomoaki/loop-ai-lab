Epic: "Epic 2: Application UseCase & Business Logic"
Sprint: 1
TaskName: "Implement Avatar Generation UseCase and Domain Orchestration"
TargetFiles:
  - "workspace/avatar-service/internal/application/usecase/avatar_usecase.go"
  - "workspace/avatar-service/internal/application/usecase/avatar_usecase_test.go"
AcceptanceCriteria:
  - "Define the `AvatarUseCase` interface with method signature `Generate(seed string) ([]byte, error)` in `internal/application/usecase/avatar_usecase.go`."
  - "Implement the private `avatarUseCase` struct holding the domain port dependency `renderer repository.AvatarRenderer`."
  - "Implement the provider constructor `NewAvatarUseCase(renderer repository.AvatarRenderer) AvatarUseCase` returning the interface."
  - "Orchestrate domain entity creation via `model.NewAvatar(seed)` and delegate PNG rasterization to `renderer.Render(avatar)`."
  - "Ensure strict adherence to Go Clean Architecture: Application layer imports only Domain layer (`internal/domain/model`, `internal/domain/repository`) and standard library, with zero infrastructure or third-party dependencies."
  - "Handle and propagate rendering errors gracefully without panics or leaking infrastructure-specific details."
  - "Implement comprehensive unit tests in `internal/application/usecase/avatar_usecase_test.go` using a mock implementation of `repository.AvatarRenderer`."
  - "Verify test vectors covering valid strings, empty seeds, whitespace strings, and multibyte UTF-8 characters (e.g., Japanese, Emoji)."
DoD:
  - "`avatar_usecase.go` and `avatar_usecase_test.go` are placed strictly within `internal/application/usecase/`."
  - "Unit test suite achieves 100% statement and branch coverage for the UseCase package."
  - "`go test -race ./internal/application/usecase/...` passes with zero failures and zero race conditions."
  - "`go vet ./internal/application/usecase/...` and linter checks pass with zero warnings."
